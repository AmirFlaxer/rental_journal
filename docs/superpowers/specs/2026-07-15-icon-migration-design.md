# מעבר מאמוג'י לאייקוני Lucide - Design Spec

> תאריך: 2026-07-15 · פריט "גל-2" מ-docs/superpowers/specs/2026-07-10-paper-edition-redesign-design.md §5 (דיון-זהות נפרד)

## 1. רקע ומניע

האפליקציה משתמשת כיום באמוג'י (61 גליפים ייחודיים, פרוסים ב-20+ קבצים) לניווט, סימוני-קטגוריה, סטטוס ופעולות. המניע למעבר: **מקצועיות/רצינות** - זו אפליקציית ניהול-כספים (שכ"ד, מס, חוזים) ואמוג'י צבעוניים-משתנים-בין-פלטפורמות פוגעים בתחושת האמינות. הכרעת-אמיר: החלפה מלאה, כולל אלמנטים דקורטיביים.

## 2. היקף

**בפנים**: כל 61 הגליפים ב-20 הקבצים שנסרקו (נספח א'), כולל:
- ניווט (סרגל שולחני + bottom-nav מובייל)
- קטגוריות תזכורות/הוצאות (גז, מים, ביטוח, שכ"ט מקצועי...)
- סטטוס-תשלום ("מיני-רמזור" קיים בעמוד-נכס ובדוח-נכס)
- פעולות (מחיקה, עריכה, אישור, ביטול, הוספה)
- דקורטיביים (חלקם יורדים, חלקם מקבלים מקבילה פונקציונלית - ראו §5)

**בחוץ (backlog נפרד, לא כאן)**: rent-roll רמזור מלא (עיצוב-תצוגה חדש, לא רק החלפת-סמל), דיגסט Push תקופתי, bottom sheets, תמונות-נכסים.

## 3. ארכיטקטורה

- **חבילה**: `lucide-react` (tree-shakeable, import בודד לכל אייקון - לא משפיע על bundle).
- **`src/lib/icons.ts`**: מפה `Record<IconName, LucideIcon>` יחידה - כל שם לוגי (למשל `"properties"`, `"gas"`, `"delete"`) ממופה לקומפוננטת-Lucide אחת. שינוי-אייקון עתידי = שורה אחת במקום אחד.
- **`<Icon name="..." size={20} className="..." color="..." />`**: קומפוננטת-עטיפה ב-`src/components/Icon.tsx`, `strokeWidth={1.75}` כברירת-מחדל.
- **תיקון-אגב**: `NAV_ITEMS` כיום מוגדר פעמיים (`src/app/dashboard/layout.tsx` - מערך שולחני + מערך bottom-nav נפרד עם אותם ערכים). מתאחד ל-`src/lib/nav-items.ts` כמקור-אמת יחיד; שני הרכיבים צורכים אותו.
- **תיקון-אגב נוסף**: סטטוס-תשלום "עתידי" מסומן היום ב-📅 בעמוד-נכס (`properties/[id]/page.tsx:1000`) וב-🔲 בעמוד-דוח (`reports/[propertyId]/page.tsx:327`) - שני סמלים שונים לאותה משמעות. מאוחד ל-`Calendar` בשני המקומות.

## 4. צבע סמנטי, גודל, עובי-קו

- **צבע**: כל אייקון יורש `currentColor`; ההקשר קובע צבע לפי הכללים הסמנטיים הקיימים (ירוק=הכנסה/paid, אדום=הוצאה/debts/overdue, ענבר=מס/partial/attention-בינונית). אייקוני-ניווט נייטרליים נשארים בגוון-דיו (`--text-2`) עם הדגשת-מותג (`--accent`) כשפעילים - כמו היום.
- **גודל**: 20px בניווט, 18px בכרטיסי-סטטוס, 16px בטקסט-מוטבע/כפתורים קטנים.
- **עובי-קו**: `strokeWidth={1.75}` (עדין יותר מברירת-המחדל 2, תואם לזהות "דיו על קלף" העדינה).

## 5. RTL וגליפים דקורטיביים

- **בדיקת-כיווניות**: נסרקו כל מופעי `→`/`←` בקוד - **כולם בתוך הערות-קוד** (`// A → B` = "הופך ל"), **אין אייקון-ניווט כיווני אמיתי ב-UI** (אין חצי דפדוף/פאג'ינציה עם אמוג'י). לכן **אין צורך בהיפוך-RTL** במעבר הזה. `↩`(LogOut) `☰`(Menu) `↻`(RotateCw) אינם כיווניים סמנטית - מיפוי ישיר בלי טיפול-RTL.
- **גליפים דקורטיביים-רגשיים** (הוחלט 2026-07-15):
  - `👋` ("שלום 👋" בברכת-דשבורד) - **יורד לגמרי**. טהור-אישיות, אין מקבילת-קו הגיונית, ואין איתות-פונקציונלי לשמר. נשאר "שלום, אמיר".
  - `✨` (חילוץ-AI, בקשת-פיצ'ר) → **`Sparkles`** - קונבנציית-UI מוכרת ל"AI-powered"/"חדש", לא דקורטיבי-גרידא.
  - `🐞` (דיווח-באג, זוג-קטגוריה מול ✨) → **`Bug`**.
  - `🗞️` ("מאז הביקור האחרון") → **`Newspaper`** - מטאפורת "יומן/עיתון" תואמת בדיוק לזהות "יומן הספר".
  - `💭` ("המודל חושב...") → **`Brain`**.

## 6. בדיקת-שפיות (vitest)

טסט קטן שעובר על כל שם-לוגי הנצרך בפועל בקוד (`grep` על שימושי `<Icon name=`) ומוודא שהוא קיים ב-`src/lib/icons.ts` - רשת-ביטחון זולה נגד טעות-הקלדה שתגרום לרינדור-ריק, בהינתן 61 מיפויים ב-20+ קבצים.

## 6.1 גילוי מבדיקה-חזותית: אייקוני-כסף עם מטבע אפויה בצורה

בדיקה חזותית בקומפניון (Lucide בפועל, לא ASCII) העלתה ש-`Receipt` מצייר סימן **"$"** כחלק מקו-הצורה עצמו (לא רק "מרגיש אמריקאי" - זה פיזית חלק מה-SVG). Lucide כולל משפחת `Receipt*` למטבעות ספציפיות (יורו/ין/פאונד/רובל/פרנק/לירה-טורקית) **אך לא לשקל**. הוחלט (אמיר, על סמך תצוגה חיה):
- **הוצאות + קבלה/חשבונית כללית**: `Receipt` → **`ReceiptText`** (אותה צורת-קבלה, קווי-טקסט כלליים במקום "$").
- **תקבולים**: `CreditCard` (לא-מתאים - האפליקציה מתבססת על שיקים, לא כרטיסי-אשראי) → **`HandCoins`** (יד מקבלת-תשלום, לא מתחייבת לאמצעי ספציפי).
- **לקח לעתיד**: לפני קביעת אייקון-כסף מ-Lucide יש לבדוק את ה-SVG-path בפועל (לא רק שם-האייקון) - סמלי-מטבע עלולים להיות אפויים לתוך הצורה.

## 7. בדיקה חזותית

שינוי-רוחב שנוגע בכל מסך - Playwright fullPage (סקיל `verify` הפרויקטלי) על כל המסכים אחרי המעבר: דשבורד, נכסים (+עמוד-פרט), חוזים (+ייבוא +עריכה), הוצאות, תקבולים, דוחות (+דוח-מס +דוח-נכס), חובות, תזכורות, אודות, הגדרות, תחזוקה. לב לשים: יישור-אנכי של אייקון מול טקסט עברי (line-height), וצבע-סמנטי נכון בכל הקשר.

## 8. קריטריוני הצלחה

1. אפס אמוג'י נותר בקוד (`grep` ריק על טווח-היוניקוד של אמוג'י ב-`src/`, מלבד תוכן-משתמש חופשי כמו notes).
2. `NAV_ITEMS` מאוחד למקור אחד; סטטוס "עתידי" מאוחד לאייקון אחד בשני המסכים.
3. כל אייקון בצבע-סמנטי נכון (בדיקה חזותית).
4. tsc/lint/vitest/build ירוקים; פריסה אוטומטית ירוקה.
5. אין רגרסיה בדוח המס המודפס (print) - אייקונים לא-רלוונטיים ל-print ממילא, אך לוודא שלא נשברה עימוד.

## נספח א' - טבלת מיפוי מלאה (61 גליפים)

### ניווט (`src/lib/nav-items.ts`)
| גליף | פריט | Lucide |
|---|---|---|
| 🏠 | לוח בקרה | `Home` |
| 🏢 | נכסים | `Building2` |
| 📄 | חוזים | `FileText` |
| 📥 | ייבוא חוזה | `Import` |
| 💸 | הוצאות | `ReceiptText` |
| 💳 | תקבולים | `HandCoins` |
| 📊 | דוחות | `BarChart3` |
| 📋 | דוח מס | `ClipboardList` |
| 🔴 | חובות | `AlertCircle` (rose) |
| 🔔 | תזכורות | `Bell` |
| ℹ️ | אודות | `Info` |
| 🔧 | תחזוקה | `Wrench` |
| ⚙ | הגדרות | `Settings` |
| ↩ | התנתקות | `LogOut` |
| ☰ | תפריט מובייל | `Menu` |

### קטגוריות תזכורות (`CAT_ICON` ב-tasks.tsx)
| גליף | קטגוריה | Lucide |
|---|---|---|
| 🛡️ | ביטוח | `ShieldCheck` |
| 💰 | גביית שכ"ד | `Wallet` |
| 📋 | חידוש חוזה | `FileClock` |
| 🔧 | תחזוקה | `Wrench` |
| 📊 | מס | `Percent` |
| 🔥 | גז | `Flame` |
| 💧 | מים | `Droplet` |
| ⚡ | חשמל | `Zap` |
| 🏛️ | ארנונה | `Landmark` |

### קטגוריות הוצאות (`CAT_ICON` ב-expenses/page.tsx)
| גליף | קטגוריה | Lucide |
|---|---|---|
| 👔 | שכ"ט מקצועי | `Briefcase` |
| 📦 | אחר | `Package` |

### סטטוס-תשלום ("מיני-רמזור" - properties/[id], reports/[propertyId])
| גליף | סטטוס | Lucide |
|---|---|---|
| ✅ | התקבל | `CheckCircle2` (emerald) |
| 🔶 | חלקי | `CircleDot` (amber) |
| ⚠️ | לא התקבל | `AlertTriangle` (rose) |
| 📅 / 🔲 | עתידי (מאוחד) | `Calendar` (ניטרלי) |
| ❌ | באיחור/לא-שולם | `XCircle` (rose) |
| ⛔ | חוזה פג-תוקף | `Ban` (rose) |
| 🔴 | ≤30 יום לפקיעה | `AlertCircle` (rose) |
| 🟡 | 31-90 יום לפקיעה | `AlertCircle` (amber) |
| 🚪 | סיום מוקדם | `DoorOpen` |

### ארכיון/סינון
| גליף | הקשר | Lucide |
|---|---|---|
| 🗂 | טאב "ארכיון" (leases) | `Archive` |

### תחזוקה (maintenance/page.tsx)
| גליף | פעולה | Lucide |
|---|---|---|
| 🧹 | ניקוי (תזכורות/חוזים) | `Eraser` |
| 🔍 | בדיקת תקינות | `SearchCheck` |

### טפסי ייבוא/עריכת-חוזה
| גליף | הקשר | Lucide |
|---|---|---|
| 👤 | שוכר יחיד | `User` |
| 👥 | שוכר שני/כמה | `Users` |
| 📝 | הערה (הוצאות) | `StickyNote` |
| 📝 | מסמך לא-PDF | `File` |
| 📎 | מסמך מצורף | `Paperclip` |
| 🖨 | הדפסה | `Printer` |

### אודות (יצירת-קשר)
| גליף | הקשר | Lucide |
|---|---|---|
| 💻 | קישור GitHub | `ExternalLink` (Lucide לא כולל לוגו-מותגים) |
| ✉️ | מייל | `Mail` |

### דקורטיביים/סטטוס-AI (ראו §5)
| גליף | הקשר | Lucide |
|---|---|---|
| 🐞 | דיווח באג | `Bug` |
| ✨ | חילוץ-AI / בקשת-פיצ'ר | `Sparkles` |
| 🗞️ | פיד "מאז הביקור" | `Newspaper` |
| 💭 | המודל חושב | `Brain` |
| 👋 | ברכה | **מוסר** |

### פעולות כלליות
| גליף | פעולה | Lucide |
|---|---|---|
| ✓ / ✅ | בוצע | `Check` / `CheckCircle2` |
| ✕ / ❌ | ביטול | `X` / `XCircle` |
| 🗑 | מחיקה | `Trash2` |
| ✏ | עריכה | `Pencil` |
| 📌 | נעוץ/מודגש | `Pin` |
| 🔄 | סנכרון | `RefreshCw` |
| ↻ | רענון | `RotateCw` |
| ➕ | הוספה | `Plus` |
| ⚠️ | אזהרה כללי | `AlertTriangle` |
| 🧾 | קבלה/חשבונית | `ReceiptText` |
| 📅 | תאריך (כללי) | `Calendar` |
