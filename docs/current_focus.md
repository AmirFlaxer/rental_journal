# Current Focus - Rental Journal

<!-- נקודת-הכניסה לסשן הבא. לעדכן בסיום כל סשן (סקיל close-session אוכף זאת).
     חלוקת-העבודה מול המסמכים האחרים, כדי שלא ייווצר כפל-אמת:
       • כאן        - מה עושים **עכשיו**, מה סגור ואין לפתוח, ומה המסוכן.
       • SPEC.md    - מצב הפרויקט, ארכיטקטורה והחלטות. הוא גובר על הקובץ הזה בסתירה.
       • TODO.md    - משימות פתוחות מדורגות.
       • docs/session-log.md - היסטוריה בלבד.
     מצב הפרויקט **אינו** ב-~/.claude/current_focus.md (הועבר לכאן בקומיט eeb1bba,
     כי הקובץ ההוא חוצה-פרויקטים). אל תכתוב מצב rental_journal לשם. -->

**עודכן:** 27/7/2026, בסוף סשן ארוך (באג-PWA · שם חדש · מודול-השקים נפרס · תזכורות-שירות).

## ⭐ הפריט הבא בתור: "עזיבה ופינוי" - דורש brainstorming לפני קוד

זה הפער הפתוח היחיד ב-SPEC שהוא פיצ'ר ולא היגיינה.

**הפער המדויק:** [terminate/route.ts:22](../src/app/api/leases/[id]/terminate/route.ts#L22) חוסם
**כל** סיום מוקדם כשהחוזה מוגן (`early_term_protection` מחזיר 400), ואחרת מחשב
`תאריך-בקשה + חודשי-הודעה` - כלומר **מניח עזיבה מוסכמת**. לביטול-חוזה **בשל-הפרה** אין שום
מסלול בקוד, וזה בדיוק המקרה שבשבילו נבנתה שרשרת-ההחזרות של השקים כראיה. המודול בנה את הראיה
ואין לה לאן להוביל.

**מה שה-spec של תזכורות-השירות דחף לכאן במפורש:** החזרת פיקדון · קריאת מונים · מסירת מפתחות.

**הכניסה לסשן החדש:** `בוא נעשה brainstorming על עזיבה ופינוי.`

## מה סגור ואין לפתוח מחדש

- **השם "ניהול שכירויות"** (`short_name` "שכירויות"). "יומן הספר" ירד בהכרעת-אמיר 26/7 -
  אין להחזירו. הזהות החזותית ("מהדורת נייר") לא השתנתה.
- **מודול השקים-החוזרים פרוס**, וארבע השאריות שלו נסגרו.
- **תזכורות-השירות פרוסות**, כולל מיגרציית-הדאטה: 4 חשבונות הוגדרו, 90 שורות נמחקו,
  והמסד ירד מ-102 משימות ל-12. כל מה ששרד מהמחולל הישן הוא היסטוריה מסומנת-בוצע.
- **באג הדף-הלבן ב-PWA** - השורש היה ב-WebAPK המותקן ולא בקוד. הותקן מחדש ואומת.
- **`anchor_day` תומך בתדירות שנתית בלבד** - החלטת אפיון. דו-חודשי מעוגן ל-1 בחודש, וזה
  מה שהוריד את "מי אביבים" מיום 18 ליום 1. אין להציג את זה כבאג.

## שתי שאלות שממתינות להכרעת אמיר (לא באגים)

1. **חוזה בסטטוס `paused` נחשב "נכס ריק"** (`lease-status.ts` ממפה אותו ל-`ended`), ולכן
   מייצר חלון-ריקות מלא ותגיות "נכס ריק". שאלת-דומיין: האם שוכר במצב-השהיה הוא נכס ריק?
2. **שני חצים (`→`) בהערות קיימות ב-`tasks/page.tsx`** (שורות 159, 668) מפרים את כלל ה-RTL.
   לא נגעתי בהם כי היו מחוץ לתחום; תיקון של דקה.

## מצב מחוץ לריפו (הקוד לא מספר את זה)

- **ה-PWA במכשיר של אמיר:** חבילה `org.chromium.webapk.ae0d31e0a95b66cc1_v2`, כיתוב
  "שכירויות", נצרב `startUrl=/dashboard` ו-`standalone`. **שינוי שם/אייקון/start_url ב-manifest
  לא יגיע למכשיר בלי התקנה-מחדש** (ראה LESSONS.md על מלכודת ה-WebAPK הקפוא).
- **⚠️ ניפוי-באגים ב-USB נשאר מופעל בטלפון** מאז אבחון באג-הדף-הלבן (27/7). כל עוד הוא דלוק,
  מחשב מחובר-ומאושר יכול לקרוא ולכתוב במכשיר. שווה להזכיר לאמיר לכבות: הגדרות ← אפשרויות
  למפתחים ← ניפוי באגים ב-USB.
- **המיגרציה `20260726_utility_anchor_day.sql` הורצה בפרודקשן 27/7** (אישור אמיר), ומתועדת
  בטבלת-הסטטוס ב-`supabase/migrations/README.md`.

## המסוכן, אם נוגעים בו

- **מיגרציה חייבת לרוץ בפרודקשן לפני שהקוד שמשתמש בה נפרס.** דחיפה ל-main פורסת אוטומטית.
  הפריט הפתוח של אטומיות `bounce/route.ts` (פונקציית-Postgres) הוא בדיוק מלכודת כזו.
- **`utilityMonthWindow` נמדד מול שנת "היום"** - זה מה שמונע את חזרת תזכורות-2049.
  אין להסיר את ההגבלה לשנה הנוכחית.
- **אין למחוק שורות-דאטה בלי לקרוא אותן קודם.** בסשן הזה "72 שורות מיושנות" היו בפועל 96,
  והן החזיקו את המידע היחיד על תאריך חידוש הביטוח ושמות הספקים.
