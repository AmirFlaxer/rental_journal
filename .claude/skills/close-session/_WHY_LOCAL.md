# למה הסקיל הזה מקומי ולא גלובלי (27/7/2026)

**המקור:** `work_allocator/.claude/skills/close-session/`. הסקיל נוצר שם ב-20/7 כגלובלי
והוקפא לפרויקט **בגלל התנגשות-טריגרים**: הוא נדרך על אותן מילים בדיוק כמו
`session-closeout` של public_info - "סיימתי" / "מספיק להיום" / "הפסקה". שני סקילים
שמתחרים על אותו טריגר גורמים לאחד להישמט בלי שמישהו ישים לב.

`_WHY_FROZEN.md` שלצידו אומר במפורש מה הדרך הנכונה להביא אותו לפרויקט נוסף:
"להחזיר לתיקייה מקומית של אותו פרויקט (`<project>/.claude/skills/`) ולא לגלובלי,
ולתקן את שם-הענף". זה מה שנעשה כאן.

**מה הותאם ל-rental_journal:**
- מקום-השמירה הפרויקטלי הוא **`docs/current_focus.md` שבריפו**, ולא `~/.claude/current_focus.md` -
  מצב הפרויקט הועבר לריפו בכוונה בקומיט `eeb1bba`.
- נוספו `SPEC.md §הצעד הבא`, `TODO.md`, `docs/session-log.md` ו-`supabase/migrations/README.md`
  כמקומות-שמירה נוספים לבדיקה.
- בדיקת ה-CI דורשת **את שני ה-jobs** (`checks` ו-`deploy`), כי דחיפה ל-main פורסת לייצור.
- שם-הענף נגזר ב-`git branch --show-current` ואינו מקובע.
- נוספו חמש מלכודות שנצפו בפועל בפרויקט הזה (build כגייט-היחיד לגבול-client, מיגרציות ידניות
  ו-`gen:types`, `package-lock` שמתלכלך ב-Windows, ושרת מקומי ששורד).

**הערה על גיבוי:** במקור הסקיל יושב במחיצה gitignored ולכן אינו מגובה. **כאן הוא כן מגובה** -
`.gitignore` של הפרויקט מחריג `!.claude/skills/`, כך שהסקיל נכנס לריפו ונדחף.
